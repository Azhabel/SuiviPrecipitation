Name: Manage Integration
on: [push]
jobs :
  build:
    docker :
      - image: docker:3.10-alpine3.15

    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: Build
          command: docker build -t test .

      - run:
          name: Run
          command : docker run -d -p 8080:8080 --name test_container test 
